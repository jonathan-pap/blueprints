# Wireframe & Story Brief (template)

> The inputs an AI needs to draft a **story arc + page wireframes**. Extends the report brief
> ([`../brief-template.md`](../brief-template.md)) with the story/layout specifics. Copy this, fill the
> blanks, save as `projects/<name>/wireframe-brief.md`. Fewer blanks = fewer questions before you get a draft.

Replace every `<…>`; delete _italic hints_.

## 1. The decision (why this report exists)

- **The one decision it drives:** `<e.g. which regions to prioritise for Q3 investment>`
- **Who decides / who reads:** `<role(s), data-literacy, on desktop or mobile>`
- **When / where used:** `<e.g. Monday ops review on a big screen | self-serve on laptops>`

## 2. The questions (in the order they'd be asked)

_The report should walk the reader through these. 3–7 is ideal._

1. `<e.g. Are we up or down vs plan overall?>`
2. `<e.g. Which regions/products drive the gap?>`
3. `<e.g. Is it price, volume, or mix?>`
4. `<e.g. Which specific accounts need action?>`

## 3. Key numbers (KPIs / headline measures)

- **Headline KPIs (the summary row):** `<e.g. Total Sales, Margin %, vs Target, YoY %>`
- **Supporting measures:** `<e.g. Units, Discount %, Sales by Region/Product>`
- **Comparisons / context:** `<vs target | vs PY | vs forecast | rank>`

## 4. Story arc

- **Preferred arc:** `<Overview→Analysis→Detail | Martini glass | Drill path | let AI recommend>`
- **Approx page count:** `<e.g. 3–4>`
- **Must-have pages:** `<e.g. Exec summary; Region deep-dive; Account detail>`
- **Entry point:** `<which page opens first>`

## 5. Layout constraints

- **Canvas:** `<1280×720 (16:9 default) | other>` · **Device:** `<desktop | mobile layout too>`
- **Navigation:** `<page tabs | buttons/bookmarks | single scroll page>`
- **Must-fit elements:** `<company logo, filter rail, last-refresh stamp, legal footer…>`
- **Branding/tone:** `<link the design-identity or theme if decided; else "TBD">`
- **Filters:** `<global slicers? which fields? persistent rail or a filter page?>`

## 6. Data reality (so placeholders are feasible)

- **Model known?** `<yes → see projects/<name>/*.model-context.md | no>`
- **Fields/measures that exist:** `<list, or point to the Model Context Brief>`
- **Grain / date range:** `<e.g. daily, last 2 years>`
- **Gaps:** `<anything asked for that the data can't answer yet>`

## 7. Output & review

- **Fidelity:** `<text wireframe only | + HTML/SVG mockup for stakeholders>`
- **Who signs off the wireframe:** `<name/role>`
- **Definition of done:** `<e.g. every page answers a Q2 question; sign-off before build>`
