# theme/promote — lift visual-level overrides into the theme

- `clear-visual-overrides.md` — Clear redundant visual overrides: After promoting an override to the theme (`from-visuals.md`), strip it from every visual that was setting it.
- `from-visuals.md` — Promote visual-level formatting into the theme: When 3+ visuals of the same type share an override, lift it to a theme-level rule. Cleaner visual JSON, single…
