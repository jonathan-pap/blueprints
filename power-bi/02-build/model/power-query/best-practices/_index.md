# power-query/best-practices — M authoring rules

- `anti-patterns.md` — Anti-patterns: Common M mistakes and what to do instead.
- `column-pruning.md` — Column pruning: Remove columns as early as possible. Every column not removed travels through every subsequent step — and fold…
- `error-handling.md` — Error handling: For production partitions, **avoid `try...otherwise` patterns that silently swallow errors.** A failed refresh…
- `naming-conventions.md` — Naming conventions: Power Query's UI auto-generates step names like `#"Removed Columns"`, `#"Filtered Rows"`. Manual edits like `C…
- `query-folding.md` — Query folding: The most important Power Query performance concept. The M engine translates compatible steps into native data …
- `row-filtering.md` — Row filtering: Filter rows early. A `Table.SelectRows` immediately after navigation folds to `WHERE` — the source returns onl…
- `type-handling.md` — Type handling: `Table.TransformColumnTypes` folds to `CAST` in SQL. Apply right after column selection.
