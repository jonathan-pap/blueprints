# fix-pattern/ — common bug recipes

- `summarize-by-key.md` — key column wrongly summing → flip to `none`
- `missing-description.md` — add a `///` description correctly
- `format-string-by-type.md` — pick the right format pattern per data type
- `dynamic-format-string.md` — replace `formatString` with `formatStringDefinition`
- `m-table-namespace-collision.md` — fix the "duplicate member" load error
- `pbi-format-hint-readded.md` — why this annotation keeps coming back

Each is a one-page recipe: symptom, fix, why.
