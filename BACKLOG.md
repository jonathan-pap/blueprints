# Backlog

Work intentionally **not shipped** — kept on disk but excluded from git. Parked *decisions* (design
direction to resume later) live in `memory/`, not here; this file is only "code on disk we chose not to
sync."

## Not ready to ship — git-ignored

- **candlestick (OHLC + volume-overlay) recipe** — `power-bi/02-build/recipes/candlestick/`. Not
  validated/finalized; ship once proven against a real report. Referenced by `.gitignore` and
  `power-bi/ATTRIBUTIONS.md`.

> This is in `.gitignore`. Local files remain; they are simply not synced to `origin`.
